import styled from 'styled-components'
import { colors } from '../../../GlobalStyles'

export const StyledPlatformSettings = styled.div`
  width: 100%;

  h4 {
    text-transform: uppercase;
    color: ${colors.textColor};
    font-size: 0.8rem;
    font-weight: 700;
    margin: 2em 0 1.5em;
  }
`
